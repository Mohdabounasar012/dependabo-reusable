name: dependabot call

on:
  workflow_call:
    inputs:
      message:
        description: 'Message to print'
        required: false
        default: 'Hello from reusable workflow!'
        type: string

jobs:
  say-hello:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Print message
        run: echo "${{ inputs.message }}"
